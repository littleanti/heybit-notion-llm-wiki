# 비밀값
.env
.env.local
.env.*.local
# setup --set-token 이 쓰는 임시 토큰 파일 (정상 경로에서는 즉시 지워지지만 방어선을 하나 더 둔다.
# *.tmp 는 아래 "임시" 절에 이미 있다)
*.token
token*.txt
notion-token*

# Node
node_modules/
npm-debug.log*

# 임시
tmp/
*.tmp

# OS / 에디터
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# oh-my-claudecode 세션 상태 (운영 아티팩트, 프로젝트 산출물 아님)
.omc/

# 참고: raw/.sync-state.json 과 wiki/.publish-state.json 은 의도적으로 커밋한다 (docs/TRD.md 4절)

# 실 워크스페이스 시딩 상태 (실제 페이지 id — 재현용이 아니다)
.seed-state.json
